pipeline{
    agent any
    tools{
       maven'M2_HOME'
    }
        stages{
            stage("all maven commands"){
                steps{
                    sh 'mvn clean compile install package'
                }
            }
            stage("Sonarqube scan"){
                 steps{
                    sh 'mvn verify org.sonarsource.scanner.maven:sonar-maven-plugin:sonar -Dsonar.projectKey=amankaur655_geo'
                  }
             }
        }
    
}
